## 重构摘要
- 目标：完成第七批旧公共头脚本迁移，统一改为 source `bootstrap/lib/runtime.sh`；同时修复已迁移 `tools/wireguard/wgclient.sh` 在 `runtime.sh` 下 direct `_root` 调用的兼容回归
- 改动脚本：`tools/wireguard/wgclient.sh`、`tools/qbittorrent/install.sh`、`tools/qbittorrent/smb.sh`、`tools/transmission/install.sh`、`tools/transmission/transmission.sh`
- 保持不变的行为：`tools/qbittorrent/install.sh`、`tools/qbittorrent/smb.sh`、`tools/transmission/install.sh`、`tools/transmission/transmission.sh` 的命令入口、业务逻辑和帮助输出保持不变，仅删除重复脚本头并统一改为定位并 source `bootstrap/lib/runtime.sh`；`tools/transmission/install.sh` 中原先依赖旧 `_run -x "..."` 的调用，按 `runtime.sh` 当前支持的命令参数形式改写，执行语义保持为“带 trace 的直接命令执行”；`tools/wireguard/wgclient.sh` 仅把需要退出的 root 检查从 `_root` 改为 `_require_root`
- 验证结果：已执行 `bash -n tools/wireguard/wgclient.sh tools/qbittorrent/install.sh tools/qbittorrent/smb.sh tools/transmission/install.sh tools/transmission/transmission.sh`，通过；已执行 `shellcheck tools/wireguard/wgclient.sh tools/qbittorrent/install.sh tools/qbittorrent/smb.sh tools/transmission/install.sh tools/transmission/transmission.sh`，迁移新增告警未发现，剩余为迁移前已存在的历史告警；其中 `tools/wireguard/wgclient.sh`、`tools/qbittorrent/install.sh`、`tools/qbittorrent/smb.sh`、`tools/transmission/install.sh`、`tools/transmission/transmission.sh` 的历史告警均已分别通过 `git show HEAD:<file> | shellcheck -s bash -` 确认，`tools/transmission/transmission.sh` 的 `shellcheck` 解析错误仍是模板占位符 `<TransmissionDownloadDir>` 导致的历史问题
- 残余风险：`bootstrap/lib/runtime.sh` 的 `_root()` 返回码语义与旧脚本家族仍未全局收敛，后续迁移仍需继续把“必须退出”的调用点显式改为 `_require_root`；另外仍有部分旧脚本依赖更复杂的 `_run` / `_runAsRoot` 旧语义或包含可疑历史逻辑，需要继续按小批量方式逐个迁移
